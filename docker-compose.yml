services:
  cct-coffee:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cct-coffee-app
    restart: always
    ports:
      - "5173:80"
    environment:
      - NODE_ENV=production
